const settings = {
  devRunMode: 100,
  withConsole: false,
};
const hostname = `https://worksocial-yrhr.onrender.com`;

// Utilisez export direct au lieu de module.exports
export { settings, hostname };
